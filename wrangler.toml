# Cloudflare Pages configuration for Tradings
# Frontend: Next.js deployed to Cloudflare Pages
# Backend: Workers deployed separately from /workers directory

name = "tradings"
compatibility_date = "2025-07-21"

# Pages-specific configuration
[env.production]
vars = { ENVIRONMENT = "production" }

# Routes - Next.js handles all frontend routes
# API routes are handled by the Workers backend
